#include<stdio.h>
//The #include is the preprocessor the stdio.h is the header file
int main()
{
    /*this is a multi line comment 
    here the int is the return type and the main is the function */
    printf("The comments are given in this way \n we use \\\ for single line comments \n");
    printf("we use the \/* -----\*/ for the multi line comments");
    return 0;
}